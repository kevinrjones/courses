@model KelloUserViewModel

@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Login</h2>

@using (Html.BeginForm("Login", "Account"))
{
    @Html.Partial("_Credentials", Model)
    <div>
        <input type="submit" value="Log in" />
    </div>
}
